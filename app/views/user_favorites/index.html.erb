<h1>User Favorites</h1>

<table class="table table-striped table-hover">
	<thead>
		<tr>
			<th>User</th>
			<th>Artist ID</th>
		</tr>
	</thead>
	<tbody>
		<% @user_favorites.each do |user_favorite| %>
			<tr>
				<td><%= user_favorite.user_id %></td>
				<td><%= user_favorite.artist_id %></td>
			</tr>
		<% end %>
	</tbody>
</table>